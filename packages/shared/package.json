{
  "name": "@dark-docs/shared",
  "version": "1.0.0",
  "description": "Shared utilities and constants for Dark Docs",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "echo 'No build needed for shared package'",
    "dev": "echo 'No dev server for shared package'",
    "lint": "echo 'No linting for shared package'",
    "format": "echo 'No formatting for shared package'",
    "format:check": "echo 'No formatting check for shared package'"
  },
  "dependencies": {},
  "devDependencies": {}
}
