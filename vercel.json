{
  "buildCommand": "pnpm build:www",
  "outputDirectory": "apps/www/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "functions": {
    "apps/www/src/app/**": {
      "maxDuration": 30
    }
  }
}